name: build

on:
  push:
    paths-ignore: ['*.md']
  pull_request:
  workflow_dispatch:

env:
  IS_THIS_RELEASE: false
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

<<<<<<< HEAD
=======
      - name: Validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6

>>>>>>> 98e8cb2f (feat: Initial commit - Add upstream litematica-printer repository)
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'microsoft'

<<<<<<< HEAD
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Cache Loom workspace
        uses: actions/cache@v5
        with:
          path: |
            ./.gradle/loom-cache
            ./libs
          key: ${{ runner.os }}-loom-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties', '**/*.gradle.kts', '**/gradle.properties', 'buildSrc/**') }}
          restore-keys: |
            ${{ runner.os }}-loom-
=======
      - name: Cache Gradle dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ./.gradle
            ./libs
          key: ${{ runner.os }}-gradle-common-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties', '**/*.gradle.kts', '**/gradle.properties', 'buildSrc/**') }}
          restore-keys: |
            ${{ runner.os }}-gradle-common-
            ${{ runner.os }}-gradle-
>>>>>>> 98e8cb2f (feat: Initial commit - Add upstream litematica-printer repository)

      - name: Make gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build fabricWrapper subproject
<<<<<<< HEAD
        run: ./gradlew :fabricWrapper:build --stacktrace --console=plain
=======
        run: ./gradlew :fabricWrapper:build --stacktrace
>>>>>>> 98e8cb2f (feat: Initial commit - Add upstream litematica-printer repository)

      - name: Delete wrapper sources jar
        run: |
          find fabricWrapper/build/libs/ -name "*-sources.jar" -delete

      - name: Capture versionpack
        uses: actions/upload-artifact@v7
        with:
          name: versionpack
          path: fabricWrapper/build/libs/*.jar
          if-no-files-found: error
          retention-days: 1
          archive: false

      - name: Capture standalone version JARs
        uses: actions/upload-artifact@v7
        with:
          name: version-jars
          path: fabricWrapper/build/libs/jars/
          if-no-files-found: error
          retention-days: 1
